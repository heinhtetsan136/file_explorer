const String keyForGetTheme = "theme";

enum AppThemeMode { light, dark, system }

const Map<AppThemeMode, String> themeMode = {
  AppThemeMode.light: "light",
  AppThemeMode.dark: "dark",
  AppThemeMode.system: "system",
};
